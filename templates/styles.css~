li {
	display: inline;
}
section {
        float: left;
        max-width: 49%;
        margin: 0;
        padding: 1em;
        overflow: hidden;
      }

      article {
        margin-left: 50%;
        border-left: 1px solid gray;
        padding: 1em;
        overflow: hidden;
      }
